function arr1(){


    let number=[1,2,3,4,];

    // number.pop()

    // number.at(1) ===== //2

    // number.push(7) ===== add element to the end : [1,2,3,4,7]

    // number.fill(1) ==== fiil every Element

    // number.shift() ==== remove first Element

    // number.reverse() ====sort in descending order 

    // number.includes(5)  === Array contains

    // number.toString();

    // number.join( "*")

    // // number.splice( 2,0,"i","p");

    // number.slice (1,4);

    // number.sort();

     console.log(number)

}
arr1();